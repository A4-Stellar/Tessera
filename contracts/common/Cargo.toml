[package]
name = "tessera-common"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "Shared pause/upgrade storage helpers for the Tessera Soroban contracts."

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = { workspace = true }
