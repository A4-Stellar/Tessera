[{"id":0,"route":"/docs/api/assets","title":"API · Assets","headers":"Assets List assets Get an asset","content":"\r\n\r\n# Assets\r\n\r\n<CalloutBox variant=\"note\">\r\nReplace <code>$API_BASE_URL</code> in examples with your configured API base URL (see <a href=\"/docs/api/overview\">API Overview</a>).\r\n</CalloutBox>\r\n\r\n## List assets\r\n\r\n<ApiEndpoint method=\"GET\" path=\"/assets\" description=\"All tokenized assets with valuation, supply and holder counts.\" />\r\n\r\nReturns an array of asset objects.\r\n\r\n**Query parameters** (all optional)\r\n\r\n| Name | Type | Notes |\r\n|------|------|-------|\r\n| `asset_type` | string | Filter by class: `real_estate`, `invoice`, `commodity` |\r\n| `active` | boolean | Filter by active status |\r\n| `fields` | string | Sparse fieldset: comma-separated list such as `id,name,active`. |\r\n| `offset` | integer | Skip the first N matching assets. Default: `0`. |\r\n| `limit` | integer | Page size. Default: `50`; maximum: `100`. |\r\n\r\nResults are paginated to keep large collections bounded. Use `offset` and `limit` together to walk the list in chunks.\r\n\r\n```bash\r\ncurl \"$API_BASE_URL/assets?asset_type=real_estate&active=true&offset=0&limit=50\"\r\n```\r\n\r\n**Response schema** (per asset):\r\n\r\n| Field | Type | Notes |\r\n|-------|------|-------|\r\n| `id` | number | Registry id |\r\n| `token_contract` | string | Asset-token contract |\r\n| `issuer` | string | Issuer account |\r\n| `name` | string | |\r\n| `symbol` | string | |\r\n| `asset_type` | string | `real_estate` \\| `invoice` \\| `commodity` |\r\n| `description` | string | |\r\n| `valuation_cents` | string | USD cents (`i128`) |\r\n| `valuation_usd` | number | Dollars, convenience |\r\n| `decimals` | number | |\r\n| `total_supply` | string | Base units (`i128`) |\r\n| `holders` | number | Addresses with a positive balance |\r\n| `active` | boolean | |\r\n| `paused` | boolean | |\r\n| `compliance_contract` | string | Gate for this asset |\r\n| `created_at_ledger` | number | |\r\n| `indexed_at_ledger` | number | Most recent ledger successfully indexed for this asset |\r\n| `index_error` | string or null | Per-asset index error, if the latest refresh failed |\r\n\r\n**Example**\r\n\r\n```bash\r\ncurl $API_BASE_URL/assets\r\n```\r\n\r\n```json\r\n[\r\n  {\r\n    \"id\": 1,\r\n    \"token_contract\": \"CBMCWLSQSWUTLUJFCNBHNBSXMUM3XU7NAQ5TSNERW4HA4ZZBYHLG4ECZ\",\r\n    \"issuer\": \"GAIQGTOBTTLLDJ4SWGGESM7UWJ2DI4K3ZNHUSHPDKJL2IE5FKY3BSRAA\",\r\n    \"name\": \"Manhattan Loft\",\r\n    \"symbol\": \"MLOFT\",\r\n    \"asset_type\": \"real_estate\",\r\n    \"description\": \"A tokenized loft in Manhattan\",\r\n    \"valuation_cents\": \"500000000\",\r\n    \"valuation_usd\": 5000000.0,\r\n    \"decimals\": 2,\r\n    \"total_supply\": \"1000000\",\r\n    \"holders\": 1,\r\n    \"active\": true,\r\n    \"paused\": false,\r\n    \"compliance_contract\": \"CBUERYDM7DXTZLLKDBRJKUBPFJ7M4OSUN4T7XKUARU345RLXNAIQD2IU\",\r\n    \"created_at_ledger\": 3502885,\r\n    \"indexed_at_ledger\": 3502885,\r\n    \"index_error\": null\r\n  }\r\n]\r\n```\r\n\r\n## Get an asset\r\n\r\n<ApiEndpoint method=\"GET\" path=\"/assets/:id\" description=\"Full detail for a single asset by registry id.\" />\r\n\r\n**Path parameters**\r\n\r\n| Name | Type | Notes |\r\n|------|------|-------|\r\n| `id` | number | Registry id (e.g. `1`) |\r\n\r\n**Example**\r\n\r\n```bash\r\ncurl $API_BASE_URL/assets/1\r\n```\r\n\r\nReturns a single asset object with the schema above.\r\n\r\nYou can also request a sparse fieldset with `?fields=`; for example:\r\n\r\n```bash\r\ncurl \"$API_BASE_URL/assets/1?fields=id,name,active,indexed_at_ledger\"\r\n```\r\n\r\n**Errors**\r\n\r\n```json\r\n{ \"error\": \"not_found\", \"message\": \"no asset with id 999\" }\r\n```\r\n\r\nReturned with status `404` when no asset has the given id. A non-numeric id\r\n(`/assets/abc`) is rejected with `400` by the router.\r\n\r\n<CalloutBox variant=\"tip\">\r\n`total_supply` and `valuation_cents` are strings. In JavaScript, parse them with\r\n`BigInt(asset.total_supply)` and compute a display value as\r\n`Number(BigInt(asset.total_supply)) / 10 ** asset.decimals`.\r\n</CalloutBox>\r\n"},{"id":1,"route":"/docs/api/compliance","title":"API · Compliance","headers":"Compliance","content":"\r\n\r\n# Compliance\r\n\r\n<CalloutBox variant=\"note\">\r\nReplace <code>$API_BASE_URL</code> in examples with your configured API base URL (see <a href=\"/docs/api/overview\">API Overview</a>).\r\n</CalloutBox>\r\n\r\n<ApiEndpoint method=\"GET\" path=\"/assets/:id/compliance\" description=\"Aggregate compliance summary — counts only, no addresses.\" />\r\n\r\nReturns an aggregate view of an asset's KYC allowlist. It deliberately exposes\r\n**counts, not addresses** — no personally identifying information leaves the API.\r\n\r\n**Path parameters**\r\n\r\n| Name | Type | Notes |\r\n|------|------|-------|\r\n| `id` | number | Registry id |\r\n\r\n**Response schema**\r\n\r\n| Field | Type | Notes |\r\n|-------|------|-------|\r\n| `total_records` | number | Addresses ever added to the allowlist |\r\n| `approved` | number | Stored KYC status is `Approved` — **not** the same as currently passing `is_allowed` |\r\n| `suspended` | number | |\r\n| `rejected` | number | |\r\n| `pending` | number | |\r\n| `with_expiry` | number | Records with a non-zero `expires_at` |\r\n| `jurisdictions` | array | `{ jurisdiction, count }` breakdown |\r\n\r\n**Example**\r\n\r\n```bash\r\ncurl $API_BASE_URL/assets/1/compliance\r\n```\r\n\r\n```json\r\n{\r\n  \"total_records\": 1,\r\n  \"approved\": 1,\r\n  \"suspended\": 0,\r\n  \"rejected\": 0,\r\n  \"pending\": 0,\r\n  \"with_expiry\": 0,\r\n  \"jurisdictions\": [\r\n    { \"jurisdiction\": \"US\", \"count\": 1 }\r\n  ]\r\n}\r\n```\r\n\r\n**Errors**\r\n\r\nReturns `404` with `{ \"error\": \"not_found\", … }` when the asset id is unknown.\r\n\r\n<CalloutBox variant=\"compliance\" title=\"Why counts, not addresses\">\r\nAn asset's holder set can itself be sensitive. This endpoint gives issuers and\r\nanalysts the shape of an allowlist — how many are approved, where they're based,\r\nhow many approvals carry expiries — without publishing who they are. The\r\nper-address KYC records live only on-chain and in the issuer's own tooling.\r\n</CalloutBox>\r\n\r\n<CalloutBox variant=\"warning\" title=\"Stored status vs. the enforced gate\">\r\n`approved` (and the other status counts) reflect each record's **stored**\r\n`status` field, not the on-chain `is_allowed` check the compliance contract\r\nactually enforces at transfer time. A record can be counted as `approved`\r\nhere while `is_allowed` would reject it — for example an approval that has\r\nexpired (`expires_at` in the past) or one whose `jurisdiction` has since been\r\nblocked via `is_jurisdiction_blocked`. Use `with_expiry` together with the\r\ncurrent time, and cross-reference `jurisdictions` against the contract's\r\nblocked-jurisdiction list, if you need a true count of holders currently\r\npermitted to transact. Do not treat `approved` as that number on its own.\r\n</CalloutBox>\r\n\r\nThe `jurisdictions` breakdown reflects the `jurisdiction` field of each KYC record.\r\nCombined with the contract's `is_jurisdiction_blocked`, it helps issuers audit\r\ntheir geographic exposure. See the [Compliance Guide](/docs/compliance-guide#jurisdiction-restrictions).\r\n"},{"id":2,"route":"/docs/api/dividends","title":"API · Dividends","headers":"Dividends Get a single distribution","content":"\r\n\r\n# Dividends\r\n\r\n<CalloutBox variant=\"note\">\r\nReplace <code>$API_BASE_URL</code> in examples with your configured API base URL (see <a href=\"/docs/api/overview\">API Overview</a>).\r\n</CalloutBox>\r\n\r\n<ApiEndpoint method=\"GET\" path=\"/assets/:id/dividends\" description=\"Distribution history for an asset, newest ledger first.\" />\r\n\r\nReturns every dividend distribution created for the asset's token, ordered by\r\ncreation ledger (newest first).\r\n\r\n**Path parameters**\r\n\r\n| Name | Type | Notes |\r\n|------|------|-------|\r\n| `id` | number | Registry id |\r\n\r\n**Response schema** (per distribution):\r\n\r\n| Field | Type | Notes |\r\n|-------|------|-------|\r\n| `id` | number | Distribution id |\r\n| `asset_token` | string | The asset paid out on |\r\n| `payment_token` | string | Token used to pay |\r\n| `total_amount` | string | Pool size, base units (`i128`) |\r\n| `distributed` | string | Claimed so far, base units (`i128`) |\r\n| `claimed_percent` | number | 0–100 (2 dp) |\r\n| `overflow_detected` | boolean | `true` when `distributed` exceeds `total_amount` (on-chain anomaly, not an API bug) |\r\n| `completed` | boolean | `true` once fully claimed |\r\n| `created_at_ledger` | number | |\r\n\r\n**Example**\r\n\r\n```bash\r\ncurl $API_BASE_URL/assets/1/dividends\r\n```\r\n\r\n```json\r\n[\r\n  {\r\n    \"id\": 1,\r\n    \"asset_token\": \"CBMCWLSQSWUTLUJFCNBHNBSXMUM3XU7NAQ5TSNERW4HA4ZZBYHLG4ECZ\",\r\n    \"payment_token\": \"CDLZFC3SYJYDZT7K67VZ75HPJVIEUVNIXF47ZG2FB2RMQQVU2HHGCYSC\",\r\n    \"total_amount\": \"100000000000\",\r\n    \"distributed\": \"25000000000\",\r\n    \"claimed_percent\": 25.0,\r\n    \"overflow_detected\": false,\r\n    \"completed\": false,\r\n    \"created_at_ledger\": 3510000\r\n  }\r\n]\r\n```\r\n\r\nAn asset with no distributions returns an empty array `[]`.\r\n\r\n**Errors**\r\n\r\nReturns `404` with `{ \"error\": \"not_found\", … }` when the asset id is unknown.\r\n\r\n## Get a single distribution\r\n\r\n<ApiEndpoint method=\"GET\" path=\"/assets/:id/distributions/:did\" description=\"A single distribution for an asset, by distribution id.\" />\r\n\r\n**Path parameters**\r\n\r\n| Name | Type | Notes |\r\n|------|------|-------|\r\n| `id` | number | Registry id |\r\n| `did` | number | Distribution id |\r\n\r\n**Response schema**\r\n\r\nSame shape as a single item from the list above.\r\n\r\n**Example**\r\n\r\n```bash\r\ncurl $API_BASE_URL/assets/1/distributions/1\r\n```\r\n\r\n```json\r\n{\r\n  \"id\": 1,\r\n  \"asset_token\": \"CBMCWLSQSWUTLUJFCNBHNBSXMUM3XU7NAQ5TSNERW4HA4ZZBYHLG4ECZ\",\r\n  \"payment_token\": \"CDLZFC3SYJYDZT7K67VZ75HPJVIEUVNIXF47ZG2FB2RMQQVU2HHGCYSC\",\r\n  \"total_amount\": \"100000000000\",\r\n  \"distributed\": \"25000000000\",\r\n  \"claimed_percent\": 25.0,\r\n  \"completed\": false,\r\n  \"created_at_ledger\": 3510000\r\n}\r\n```\r\n\r\n**Errors**\r\n\r\nReturns `404` with `{ \"error\": \"not_found\", … }` when the asset id or the distribution id is unknown.\r\n\r\n<CalloutBox variant=\"note\">\r\n`total_amount` and `distributed` are in the **payment token's** base units, not the\r\nasset token's. Classic Stellar assets (SACs) use 7 decimals, so `100000000000`\r\nabove is `10,000.0` units of the payment token.\r\n</CalloutBox>\r\n"},{"id":3,"route":"/docs/api/events","title":"API · Events","headers":"Events","content":"\r\n\r\n# Events\r\n\r\n<ApiEndpoint method=\"GET\" path=\"/events\" description=\"Most recent contract events captured by the indexer.\" />\r\n\r\nThe API exposes a compact event stream for clients that need to react to state changes without polling the full asset list. The payload is a JSON array ordered newest-first.\r\n\r\n```bash\r\ncurl \"$API_BASE_URL/events?limit=20\"\r\n```\r\n\r\n```json\r\n[\r\n  {\r\n    \"id\": 42,\r\n    \"contract\": \"CDL...\",\r\n    \"event_type\": \"Transfer\",\r\n    \"ledger\": 3514152,\r\n    \"timestamp\": \"2026-07-09T08:43:12.101772791+00:00\",\r\n    \"data\": {\r\n      \"from\": \"GA...\",\r\n      \"to\": \"GB...\",\r\n      \"amount\": \"100\"\r\n    }\r\n  }\r\n]\r\n```\r\n\r\nEach event includes the contract address, event name, ledger, timestamp (when available), and the raw payload emitted by the contract.\r\n"},{"id":4,"route":"/docs/api/holders","title":"API · Holders","headers":"Holders Visualize a cap table","content":"\r\n\r\n# Holders\r\n\r\n<CalloutBox variant=\"note\">\r\nReplace <code>$API_BASE_URL</code> in examples with your configured API base URL (see <a href=\"/docs/api/overview\">API Overview</a>).\r\n</CalloutBox>\r\n\r\n<ApiEndpoint method=\"GET\" path=\"/assets/:id/holders\" description=\"Holder list with balances, sorted by balance descending.\" />\r\n\r\nReturns every address that is on the asset's compliance allowlist **and** holds a\r\npositive balance. The asset-token contract does not enumerate holders directly, so\r\nthe indexer derives this from the allowlist intersected with balances.\r\n\r\n**Path parameters**\r\n\r\n| Name | Type | Notes |\r\n|------|------|-------|\r\n| `id` | number | Registry id |\r\n\r\n**Query parameters** (all optional)\r\n\r\n| Name | Type | Notes |\r\n|------|------|-------|\r\n| `offset` | integer | Skip the first N holders. Default: `0`. |\r\n| `limit` | integer | Page size. Default: `50`; maximum: `100`. |\r\n\r\n**Response schema** (per holder):\r\n\r\n| Field | Type | Notes |\r\n|-------|------|-------|\r\n| `address` | string | Holder account |\r\n| `balance` | string | Base units (`i128`) |\r\n| `share_percent` | number | Percent of total supply, 0–100 (2 dp) |\r\n\r\n**Example**\r\n\r\n```bash\r\ncurl $API_BASE_URL/assets/1/holders\r\n```\r\n\r\n```json\r\n[\r\n  {\r\n    \"address\": \"GAIQGTOBTTLLDJ4SWGGESM7UWJ2DI4K3ZNHUSHPDKJL2IE5FKY3BSRAA\",\r\n    \"balance\": \"1000000\",\r\n    \"share_percent\": 100.0\r\n  }\r\n]\r\n```\r\n\r\n**Errors**\r\n\r\nReturns `404` with `{ \"error\": \"not_found\", … }` when the asset id is unknown. An\r\nasset with holders but none currently holding a positive balance returns an empty\r\narray `[]`.\r\n\r\n<CalloutBox variant=\"note\">\r\nHolders are sorted by balance, largest first. `share_percent` is computed from\r\n`balance / total_supply` at index time, so it reflects the most recent 10-second\r\nrefresh.\r\n</CalloutBox>\r\n\r\n## Visualize a cap table\r\n\r\nThe chart below fetches live holder and compliance data for a given asset id\r\nand renders holder concentration and jurisdiction breakdowns as donut charts,\r\nwith an accessible data table underneath.\r\n\r\n<CapTableVisualizer assetId={1} />\r\n"},{"id":5,"route":"/docs/api/overview","title":"API Overview","headers":"API Overview How indexing works Operational details Authentication and rate limiting Data freshness and caching Conventions Endpoints Try it Error responses CORS `/stats` response","content":"\r\n\r\n# API Overview\r\n\r\nThe Stellar RWA API is a **read-only** REST service that indexes all tokenized\r\nasset activity on Stellar and serves it as JSON. It is written in Rust (Axum +\r\ntokio) and holds no keys — it never signs or submits transactions.\r\n\r\n**Base URL:** Set via environment variable `NEXT_PUBLIC_API_BASE_URL` (defaults to `http://localhost:8080` for development). Readers should replace examples with their configured URL.\r\n\r\n<CalloutBox variant=\"tip\">\r\nAn OpenAPI 3.0 specification is available at <code>/openapi.json</code> — use it with Swagger UI, Postman, or to generate SDK clients. See <a href=\"/docs/integration\">Integration Guide</a> for examples.\r\n</CalloutBox>\r\n\r\n## How indexing works\r\n\r\nA background task polls **Soroban RPC** every 10 seconds. On each tick it reads the\r\ncurrent state of the four contracts by *simulating* view calls — `get_all_assets`\r\non the registry, then per-asset `get_metadata`, `get_allowlist`, `balance` and\r\n`get_distributions_for_asset` — and rebuilds an in-memory snapshot.\r\n\r\n<CalloutBox variant=\"note\">\r\nState is kept **in memory** (no database in v1). If the RPC is briefly\r\nunreachable, the indexer logs the error, keeps serving the last good snapshot, and\r\nretries on the next tick. The API never panics on a transient failure.\r\n</CalloutBox>\r\n\r\n## Operational details\r\n\r\n### Authentication and rate limiting\r\n\r\nThere is currently **no authentication** required to call the API. All endpoints\r\nare open to the public.\r\n\r\n**Rate limits and HTTP caching (ETag) are being introduced.** See the dedicated\r\nreference for the full specification:\r\n\r\n<CalloutBox variant=\"tip\">\r\nSee <a href=\"/docs/api/rate-limits\">Rate Limits &amp; Caching</a> for the\r\nplanned <code>X-RateLimit-*</code> headers, <code>429</code> behaviour, ETag\r\nconditional-request flow, and polling recommendations.\r\n</CalloutBox>\r\n\r\n### Data freshness and caching\r\n\r\nThe API refreshes its in-memory state approximately **every 10 seconds** by polling\r\nSoroban RPC. This means:\r\n\r\n- Data lags the ledger by ~10 seconds on average.\r\n- Two requests made within the same 10-second window will return identical data.\r\n- If Soroban RPC is temporarily unreachable, the API continues serving the last\r\n  successful snapshot (no error, just stale data).\r\n\r\nFor integrations where you need real-time data, consider reading contracts directly\r\nvia Soroban RPC.\r\n\r\n## Conventions\r\n\r\n- **Large integers are strings.** Token amounts and valuations are `i128`\r\n  on-chain. To avoid precision loss in JavaScript, they are serialized as decimal\r\n  **strings** (`total_supply`, `valuation_cents`, `balance`, `total_amount`).\r\n- **Convenience floats.** Valuations also include a `valuation_usd` (float\r\n  dollars) and percentages (`share_percent`, `claimed_percent`) for display.\r\n- **Amounts are raw base units.** Divide by `10 ** decimals` to get a display\r\n  value. The asset's `decimals` is included on each asset.\r\n- **Cents.** `valuation_cents` is USD cents; `valuation_usd = valuation_cents / 100`.\r\n\r\n## Endpoints\r\n\r\n<ApiEndpoint method=\"GET\" path=\"/stats\" description=\"Platform-wide statistics.\" />\r\n<ApiEndpoint method=\"GET\" path=\"/events\" description=\"Recent contract events captured by the indexer.\" />\r\n<ApiEndpoint method=\"GET\" path=\"/assets\" description=\"All tokenized assets.\" />\r\n<ApiEndpoint method=\"GET\" path=\"/assets/:id\" description=\"A single asset by registry id.\" />\r\n<ApiEndpoint method=\"GET\" path=\"/assets/:id/holders\" description=\"Holder list with balances.\" />\r\n<ApiEndpoint method=\"GET\" path=\"/assets/:id/compliance\" description=\"Non-PII compliance summary.\" />\r\n<ApiEndpoint method=\"GET\" path=\"/holders/:address\" description=\"All assets held by a Stellar address.\" />\r\n<ApiEndpoint method=\"GET\" path=\"/compliance/:address\" description=\"Address compliance view across held assets.\" />\r\n<ApiEndpoint method=\"GET\" path=\"/assets/:id/dividends\" description=\"Distribution history.\" />\r\n\r\nPlus `GET /` (self-describing index) and `GET /health` (liveness).\r\n\r\n## Try it\r\n\r\n<ApiPlayground defaultPath=\"/stats\" defaultMethod=\"GET\" />\r\n\r\n## Error responses\r\n\r\nAll errors return an appropriate HTTP status code and a JSON body with a consistent\r\nformat:\r\n\r\n```json\r\n{\r\n  \"error\": \"not_found\",\r\n  \"message\": \"no asset with id 999\"\r\n}\r\n```\r\n\r\n**Error body fields:**\r\n- `error` (string): A machine-readable error code (e.g., `not_found`, `bad_request`).\r\n- `message` (string): A human-readable description of what went wrong.\r\n\r\n**Common errors:**\r\n\r\n| HTTP Status | `error` | When |\r\n|-------------|---------|------|\r\n| `404` | `not_found` | Unknown asset id or endpoint does not exist |\r\n| `400` | `bad_request` | Non-numeric `:id` path parameter or malformed query |\r\n| `500` | `internal_server_error` | Unexpected server error (rare; please report if seen) |\r\n\r\n## CORS\r\n\r\nAll origins, methods and headers are allowed, so the docs site and the web app\r\ncan call the API directly from the browser.\r\n\r\n## `/stats` response\r\n\r\n```json\r\n{\r\n  \"total_assets\": 1,\r\n  \"active_assets\": 1,\r\n  \"tvl_cents\": \"500000000\",\r\n  \"tvl_usd\": 5000000.0,\r\n  \"total_holders\": 1,\r\n  \"total_distributions\": 0,\r\n  \"last_indexed_ledger\": 3514152,\r\n  \"last_updated\": \"2026-07-09T08:43:12.101772791+00:00\"\r\n}\r\n```\r\n\r\n`total_holders` is the count of **distinct approved addresses** across all assets;\r\n`last_indexed_ledger` is the ledger the most recent successful refresh read from.\r\n"},{"id":6,"route":"/docs/api/rate-limits","title":"API · Rate Limits & Caching","headers":"Rate Limits & Caching Rate limits Response headers When the limit is exceeded HTTP caching with ETags How it works First request — capture the ETag HTTP/1.1 200 OK ETag: \"a3f9c2b1d4e7\" Cache-Control: no-cache Second request — send the ETag back HTTP/1.1 304 Not Modified   ← no body, not re-counted as a full hit TypeScript helper Which endpoints support ETags? Cache-Control Recommendations for integrators","content":"\r\n\r\n# Rate Limits & Caching\r\n\r\n<CalloutBox variant=\"note\">\r\nRate limiting and ETag support are **not yet enforced** on the current Testnet\r\ndeployment. This page documents the planned behaviour so integrators can build to\r\nthe correct contract before it goes live. The section headings will be updated and\r\nthis callout removed once the features ship.\r\n</CalloutBox>\r\n\r\n## Rate limits\r\n\r\nEach IP address is allowed **300 requests per minute** (sliding window). This\r\napplies to all endpoints equally — there are no per-endpoint overrides.\r\n\r\n### Response headers\r\n\r\nEvery response includes the following headers so clients can track their\r\nremaining budget without waiting for a `429`:\r\n\r\n| Header | Example | Meaning |\r\n|--------|---------|---------|\r\n| `X-RateLimit-Limit` | `300` | Maximum requests allowed per window |\r\n| `X-RateLimit-Remaining` | `247` | Requests remaining in the current window |\r\n| `X-RateLimit-Reset` | `1756472340` | Unix timestamp when the window resets (UTC) |\r\n\r\n### When the limit is exceeded\r\n\r\nOnce `X-RateLimit-Remaining` reaches `0`, the server responds with:\r\n\r\n```\r\nHTTP/1.1 429 Too Many Requests\r\nRetry-After: 12\r\nX-RateLimit-Limit: 300\r\nX-RateLimit-Remaining: 0\r\nX-RateLimit-Reset: 1756472340\r\n```\r\n\r\n```json\r\n{\r\n  \"error\": \"rate_limited\",\r\n  \"message\": \"too many requests — retry after 12 seconds\"\r\n}\r\n```\r\n\r\nThe `Retry-After` value (in seconds) is the time until the window resets. Back\r\noff for at least that long before retrying.\r\n\r\n<CalloutBox variant=\"tip\">\r\nFor polling use cases, read `X-RateLimit-Remaining` before every request and add\r\na short sleep when it drops below 10. The API refreshes its state every ~10 seconds,\r\nso polling faster than that does not give you fresher data anyway.\r\n</CalloutBox>\r\n\r\n## HTTP caching with ETags\r\n\r\nEvery cacheable endpoint returns a strong `ETag` derived from the content hash of\r\nthe serialized snapshot. This lets clients skip parsing a full response body when\r\nnothing has changed.\r\n\r\n### How it works\r\n\r\n1. Make a normal request — the response includes an `ETag` header.\r\n2. On the next request, send the value back in `If-None-Match`.\r\n3. If the snapshot has not changed, the server returns `304 Not Modified` with an\r\n   empty body, saving bandwidth and counting as **one request** against your rate\r\n   limit.\r\n4. If the snapshot has changed, the server returns `200 OK` with the new body and\r\n   a new `ETag`.\r\n\r\n```bash\r\n# First request — capture the ETag\r\ncurl -i $API_BASE_URL/assets/1\r\n# HTTP/1.1 200 OK\r\n# ETag: \"a3f9c2b1d4e7\"\r\n# Cache-Control: no-cache\r\n\r\n# Second request — send the ETag back\r\ncurl -i -H 'If-None-Match: \"a3f9c2b1d4e7\"' $API_BASE_URL/assets/1\r\n# HTTP/1.1 304 Not Modified   ← no body, not re-counted as a full hit\r\n```\r\n\r\n### TypeScript helper\r\n\r\n```ts\r\nconst API = process.env.NEXT_PUBLIC_API_BASE_URL ?? \"http://localhost:8080\";\r\n\r\nconst etagCache = new Map<string, { etag: string; data: unknown }>();\r\n\r\nexport async function fetchWithEtag<T>(path: string): Promise<T> {\r\n  const url = `${API}${path}`;\r\n  const cached = etagCache.get(url);\r\n\r\n  const headers: HeadersInit = cached\r\n    ? { \"If-None-Match\": cached.etag }\r\n    : {};\r\n\r\n  const res = await fetch(url, { headers });\r\n\r\n  if (res.status === 304 && cached) {\r\n    // Server confirmed nothing changed — reuse the cached body.\r\n    return cached.data as T;\r\n  }\r\n\r\n  if (!res.ok) throw new Error(`API ${res.status}`);\r\n\r\n  const data = (await res.json()) as T;\r\n  const etag = res.headers.get(\"ETag\");\r\n  if (etag) etagCache.set(url, { etag, data });\r\n\r\n  return data;\r\n}\r\n```\r\n\r\n### Which endpoints support ETags?\r\n\r\nAll read endpoints that return JSON data support ETags. The `/health` endpoint\r\ndoes **not** return an `ETag` because liveness probes should always get a fresh\r\n`200`.\r\n\r\n| Endpoint | ETag |\r\n|----------|------|\r\n| `GET /stats` | ✅ |\r\n| `GET /events` | ✅ |\r\n| `GET /assets` | ✅ |\r\n| `GET /assets/:id` | ✅ |\r\n| `GET /assets/:id/holders` | ✅ |\r\n| `GET /assets/:id/compliance` | ✅ |\r\n| `GET /assets/:id/dividends` | ✅ |\r\n| `GET /health` | ❌ |\r\n\r\n## Cache-Control\r\n\r\nThe API sets `Cache-Control: no-cache` on all responses. This tells intermediate\r\nproxies (CDNs, reverse proxies) to revalidate with the origin on every request,\r\nbut still allows ETag-based conditional requests from the client. It intentionally\r\ndoes **not** set `max-age`, since the state can change as often as every 10 seconds.\r\n\r\nIf you are fronting the API with a CDN, configure it to **forward** the\r\n`If-None-Match` / `ETag` headers so conditional requests reach the origin rather\r\nthan being absorbed by the CDN's own cache.\r\n\r\n## Recommendations for integrators\r\n\r\n- **Poll at the refresh interval.** The indexer rebuilds state every ~10 seconds.\r\n  Polling more frequently wastes budget without gaining fresher data. A 10–15 s\r\n  interval is a good default.\r\n- **Use ETags for asset detail pages.** Asset metadata rarely changes. Send\r\n  `If-None-Match` on every poll — most responses will be `304`.\r\n- **Watch `X-RateLimit-Remaining`.** Build in a backoff when the counter is low\r\n  rather than waiting for a `429`.\r\n- **Cache locally for read-heavy UIs.** If your frontend renders the same data\r\n  across multiple components, fetch once and share the result rather than making\r\n  one request per component.\r\n- **Prefer the API over direct Soroban RPC for display data.** The API reduces the\r\n  number of outbound RPC calls your integration makes and returns pre-decoded JSON.\r\n  Use direct RPC only when you need live data that has not been indexed yet.\r\n"},{"id":7,"route":"/docs/architecture","title":"Architecture Overview","headers":"Architecture Overview Repository ownership How data flows Contracts on Stellar What the API does and does not do Interface contracts","content":"\r\n\r\n# Architecture Overview\r\n\r\nThe Stellar RWA Toolkit is split across three repositories. Each owns a distinct\r\nlayer; they interact only through well-defined interfaces.\r\n\r\n```\r\n┌──────────────────────────────────────────────────────────────────────┐\r\n│                       stellar-rwa-web                                │\r\n│  (Next.js front-end — issuers & investors)                           │\r\n│                                                                      │\r\n│   • Freighter  ──sign──▶  Soroban RPC  ──submit──▶  contracts        │\r\n│   • fetch()   ──────────▶  REST API   (read-only data)               │\r\n└──────────────────────────────────────────────────────────────────────┘\r\n         ▲                          ▲\r\n         │  REST JSON               │  Soroban RPC\r\n         │                         │  simulateTransaction\r\n┌────────┴──────────┐    ┌─────────┴────────────────────────────────┐\r\n│  stellar-rwa-     │    │  Stellar network (Testnet / Mainnet)      │\r\n│  api-docs / api   │    │                                           │\r\n│  (Rust / Axum)    │    │  ┌─────────────┐  ┌──────────────────┐   │\r\n│                   │    │  │  registry   │  │  compliance      │   │\r\n│  polls every 10 s │◀───│  └─────────────┘  └──────────────────┘   │\r\n│  never signs      │    │  ┌─────────────┐  ┌──────────────────┐   │\r\n│  never submits    │    │  │  dividend   │  │  asset-token ×N  │   │\r\n└───────────────────┘    │  └─────────────┘  └──────────────────┘   │\r\n                         │           stellar-rwa-contracts            │\r\n                         └───────────────────────────────────────────┘\r\n```\r\n\r\n## Repository ownership\r\n\r\n| Repository | Language / stack | Owns | Open to contributors? |\r\n|---|---|---|---|\r\n| [`stellar-rwa-contracts`](https://github.com/A4-Stellar/stellar-rwa-contracts) | Rust · Soroban SDK | The four on-chain programs; all business logic and compliance rules | Maintainer-only |\r\n| [`Tessera`](https://github.com/A4-Stellar/Tessera) — `api/` | Rust · Axum · tokio | Read-only indexer; polls Soroban RPC, caches state in memory, serves JSON | Maintainer-only |\r\n| [`Tessera`](https://github.com/A4-Stellar/Tessera) — `docs/` | Next.js 14 · MDX | This documentation site | Open — see [CONTRIBUTING.md](https://github.com/A4-Stellar/Tessera/blob/main/CONTRIBUTING.md) |\r\n| [`stellar-rwa-web`](https://github.com/A4-Stellar/stellar-rwa-web) | Next.js · Freighter API | Issuer and investor front-end; the only layer that signs transactions | Maintainer-only |\r\n\r\n## How data flows\r\n\r\n**Writes (user → chain)**\r\n\r\n1. A user opens the web app and connects Freighter.\r\n2. The web app builds a Soroban transaction and asks Freighter to sign it.\r\n3. The signed transaction is submitted to Soroban RPC and lands on the ledger.\r\n4. The contract executes — e.g., an `asset-token` transfer triggers a\r\n   cross-contract `is_allowed` check in the `compliance` contract.\r\n\r\n**Reads (chain → API → web app)**\r\n\r\n1. The API's background indexer calls `simulateTransaction` on Soroban RPC every\r\n   10 seconds, decoding the returned `ScVal` with `stellar-xdr`.\r\n2. The decoded state is held in an in-memory snapshot (no database in v1).\r\n3. The web app — and any other client — fetches that snapshot over REST. This is\r\n   cheaper and simpler than every client simulating its own contract calls.\r\n\r\n<CalloutBox variant=\"note\">\r\nThe API is a **read-only cache**, not the source of truth. The Soroban ledger is.\r\nIf you need data fresher than ~10 seconds, simulate the relevant view call\r\ndirectly with the Stellar JS SDK — see the [Integration Guide](/docs/integration#reading-contracts-directly).\r\n</CalloutBox>\r\n\r\n## Contracts on Stellar\r\n\r\nThere are four Soroban contracts, each with a distinct responsibility:\r\n\r\n| Contract | Responsibility |\r\n|---|---|\r\n| `asset-token` | Fungible token (SEP-41) with compliance gating. One instance per RWA. |\r\n| `compliance` | Per-asset allowlist, KYC records, jurisdiction rules, expiry. |\r\n| `registry` | Global index of all tokenized assets on the platform. |\r\n| `dividend` | Escrows and distributes payment-token dividends to token holders. |\r\n\r\n`asset-token` and `compliance` are **always deployed as a pair** — each asset-token\r\nholds the address of its compliance contract and calls `is_allowed` on every\r\ntransfer. The `registry` and `dividend` contracts are **platform-wide singletons**.\r\n\r\nSee the [Contract Reference](/docs/contracts/asset-token) for full method\r\nsignatures.\r\n\r\n## What the API does and does not do\r\n\r\n| Does | Does not |\r\n|---|---|\r\n| Poll Soroban RPC and cache the result | Hold private keys |\r\n| Serve plain JSON over REST | Sign or submit transactions |\r\n| Decode XDR `ScVal` from contract view calls | Store data persistently (in-memory only) |\r\n| Provide aggregated stats (TVL, holder counts) | Enforce any access control beyond rate limiting |\r\n\r\nBecause the API never holds keys or submits transactions, it has a minimal attack\r\nsurface. All write operations go through the web app → Freighter → Soroban RPC\r\npath, where the user's wallet is the only key holder.\r\n\r\n## Interface contracts\r\n\r\nThe three layers communicate through two stable interfaces:\r\n\r\n- **Soroban contract ABI** — the method names and `ScVal` types exposed by the\r\n  four contracts. The API's `indexer/mod.rs` and the web app's SDK calls both\r\n  depend on this.\r\n- **REST API schema** — the JSON shapes documented in the [API Reference](/docs/api/overview).\r\n  The web app fetches from these endpoints; you can too.\r\n\r\nChanges to either interface are versioned. The API currently has no URL\r\nversioning (all paths are `/...`); a `v2` prefix will be added when breaking\r\nchanges are needed.\r\n"},{"id":8,"route":"/docs/compliance-guide","title":"Compliance Guide","headers":"Compliance Guide Why compliance matters for RWA How the allowlist model works How transfer gating works What a failed gate looks like Jurisdiction restrictions KYC expiry and suspension Best practices for issuers Putting it together","content":"\r\n\r\n# Compliance Guide\r\n\r\nCompliance is what separates a **real-world asset platform** from a plain token\r\nfactory. Anyone can mint a token; the hard part — and the regulated part — is\r\ncontrolling **who is allowed to hold it**. This guide explains the compliance\r\nmodel end to end: why it exists, how the allowlist works, how the on-chain\r\ntransfer gate is enforced, and how issuers should operate it.\r\n\r\n## Why compliance matters for RWA\r\n\r\nA real-world asset — equity in a building, a trade invoice, a warehouse of copper —\r\nis almost always a **regulated instrument**. The law in most jurisdictions\r\nrestricts who may own it: accredited investors only, residents of permitted\r\ncountries only, parties who have cleared KYC/AML checks only.\r\n\r\nIf those restrictions live only in a web app, they are worthless — anyone can call\r\nthe contract directly and bypass the UI. For a token to credibly represent a\r\nregulated asset, the restriction must be enforced **by the asset itself**, at the\r\nprotocol level, on every transfer. That is exactly what this platform does.\r\n\r\n<CalloutBox variant=\"compliance\" title=\"The one-sentence model\">\r\nAn RWA token can only ever be held by an address the issuer has explicitly\r\napproved — and the token enforces that on-chain, on every single transfer, by\r\nasking a separate compliance contract for permission.\r\n</CalloutBox>\r\n\r\n## How the allowlist model works\r\n\r\nThe [compliance contract](/docs/contracts/compliance) maintains a **KYC allowlist**:\r\na set of `KycRecord`s, one per address. Each record carries:\r\n\r\n- a **status** — `Approved`, `Pending`, `Rejected`, or `Suspended`\r\n- a **jurisdiction** — an ISO country code (`US`, `KE`, `DE`, …)\r\n- a **`verified_at`** ledger and an **`expires_at`** ledger (`0` = never expires)\r\n\r\nOnly the compliance **admin** (the issuer or their compliance officer) can change\r\nthe allowlist, via `add_to_allowlist`, `suspend`, and `remove`. Adding an address\r\nsets its status to `Approved`; suspending flips it to `Suspended` but keeps the\r\nrecord for audit; removing deletes it entirely.\r\n\r\nAn address is permitted to hold or receive the asset **only if `is_allowed`\r\nreturns `true`**, which requires all three of:\r\n\r\n1. status is `Approved`,\r\n2. the approval has not expired, and\r\n3. the address's jurisdiction is not blocked.\r\n\r\n## How transfer gating works\r\n\r\nThis is the mechanism that makes the whole thing enforceable. The asset-token\r\ncontract does not trust any off-chain system; on every `transfer` it makes\r\n**cross-contract calls** into the compliance contract for *both* parties:\r\n\r\n```\r\ntransfer(from, to, amount)\r\n  ├─ from.require_auth()\r\n  ├─ assert !paused\r\n  ├─ compliance.is_allowed(from)   ── cross-contract call ──▶ compliance contract\r\n  ├─ compliance.is_allowed(to)     ── cross-contract call ──▶ compliance contract\r\n  └─ move balances + emit event\r\n```\r\n\r\nIf either `is_allowed` returns `false`, the transaction **reverts** — no balance\r\nmoves, no event is emitted, the transfer simply cannot happen. The same check gates\r\n`mint` on the recipient, so tokens can never be minted into a non-approved wallet\r\nin the first place.\r\n\r\nBecause the asset token depends only on the compliance **interface**\r\n(`is_allowed`), the concrete compliance contract can be swapped with\r\n`set_compliance` — for example to migrate to a stricter policy — without\r\nredeploying the token.\r\n\r\n<CalloutBox variant=\"tip\" title=\"Verify it yourself\">\r\nTry to transfer the sample asset to an address that isn't on the allowlist. The\r\ntransaction fails with <code>RecipientNotCompliant (8)</code>. That failure is the\r\nproduct working — the gate is doing its job.\r\n</CalloutBox>\r\n\r\n### What a failed gate looks like\r\n\r\n| Contract error | Meaning |\r\n|----------------|---------|\r\n| `SenderNotCompliant (7)` | The `from` address failed `is_allowed` |\r\n| `RecipientNotCompliant (8)` | The `to` address failed `is_allowed` |\r\n| `Paused (6)` | Transfers are globally halted by the issuer |\r\n\r\n## Jurisdiction restrictions\r\n\r\nBeyond per-address control, issuers can block entire **jurisdictions**. Calling\r\n`block_jurisdiction(\"US\")` makes every approved address whose record says `US` fail\r\n`is_allowed` immediately — without editing a single individual record. Unblocking\r\nwith `unblock_jurisdiction(\"US\")` restores them.\r\n\r\nThis models a common real-world requirement: a regulatory change, sanction, or\r\noffering restriction that must apply to a whole country at once. Because the check\r\nhappens inside `is_allowed` at read time, the effect is **instantaneous and\r\nretroactive** — there is no list to re-scan and no window where stale approvals\r\nstill work.\r\n\r\nThe API's [`/assets/:id/compliance`](/docs/api/compliance) endpoint surfaces the\r\njurisdiction distribution (as counts) so issuers can audit their geographic\r\nexposure before making such a change.\r\n\r\n## KYC expiry and suspension\r\n\r\nApprovals are not forever. Two mechanisms let issuers keep the allowlist current:\r\n\r\n- **Expiry.** `add_to_allowlist` takes an `expires_at` ledger. Once the current\r\n  ledger reaches it, `is_allowed` returns `false` automatically — the holder can no\r\n  longer transfer, and cannot receive more, until re-approved. An `expires_at` of\r\n  `0` means the approval never expires. Because time is measured in **ledger\r\n  sequence numbers** (~5 seconds each), a one-year approval is roughly\r\n  `+6,300,000` ledgers from now.\r\n\r\n- **Suspension.** `suspend(address)` sets the status to `Suspended` while keeping\r\n  the record. This is the right tool for a temporary hold — a pending re-review, a\r\n  compliance flag — because it is reversible with `add_to_allowlist` and preserves\r\n  the audit trail. Use `remove` only when an address should be erased entirely.\r\n\r\n<CalloutBox variant=\"warning\" title=\"Expiry is enforced on read, not on a timer\">\r\nThere is no background job expiring records. The check is evaluated live against\r\nthe current ledger every time `is_allowed` runs. An approval that lapsed one second\r\nago fails the very next transfer — no cron, no lag.\r\n</CalloutBox>\r\n\r\n## Best practices for issuers\r\n\r\n1. **Secure the compliance admin key.** It is the single most powerful key in the\r\n   system — it decides who can hold your asset. Use multisig and hardware custody.\r\n2. **Prefer `suspend` over `remove`.** Suspension is reversible and preserves the\r\n   record for audits; removal is destructive.\r\n3. **Set sensible expiries.** Time-bounded approvals force periodic re-KYC and keep\r\n   your allowlist from silently drifting out of date. Choose an expiry that matches\r\n   your re-verification cadence.\r\n4. **Approve the issuer first.** The asset token mints the initial supply to the\r\n   admin, so the admin must be approved on the compliance contract *before*\r\n   `initialize`, or deployment fails with `RecipientNotCompliant`.\r\n5. **Coordinate distributions with balances.** The dividend contract reads balances\r\n   at **claim** time. If you need a clean snapshot, consider pausing the token\r\n   around a distribution window. See the [Dividend contract](/docs/contracts/dividend#security-considerations).\r\n6. **Use jurisdiction blocks deliberately.** They are immediate and affect every\r\n   holder in the region. Review the jurisdiction breakdown from the API first.\r\n7. **Treat registry listing as self-attestation.** Anyone can register an asset;\r\n   the *binding* control is the compliance contract, not the registry entry.\r\n\r\n## Putting it together\r\n\r\nThe compliance contract is small, but it is the reason this platform can represent\r\nregulated assets at all. Every other piece — the token, the registry, the dividend\r\nescrow, the API, the web app — is built around the single invariant it enforces:\r\n\r\n> **Only KYC-approved, unexpired, non-blocked addresses can ever hold the asset,\r\n> and the asset enforces this itself on every transfer.**\r\n\r\nNext, see how issuers operate this in practice in the\r\n[Web App Guide](/docs/web-app), or how to read compliance state programmatically in\r\nthe [Integration Guide](/docs/integration).\r\n"},{"id":9,"route":"/docs/contracts/asset-token","title":"Asset Token Contract","headers":"Asset Token Contract Data types Function reference `initialize` `transfer` `mint` `burn` `balance` / `total_supply` `pause` / `unpause` `update_valuation` `set_compliance` `get_metadata` Code example Events Error codes Security considerations","content":"\r\n\r\n# Asset Token Contract\r\n\r\n<CalloutBox variant=\"note\">\r\nThis documentation is validated against the contract specification in <code>docs/lib/contracts.ts</code>. Method signatures must match the spec — if the contract changes, update both the docs and the spec.\r\n</CalloutBox>\r\n\r\nA compliant token representing a tokenized real-world asset. Every transfer is\r\ngated by an external compliance contract: **both** the sender and the recipient\r\nmust pass `is_allowed` before any balance moves. Minting is gated on the\r\nrecipient, so only KYC-approved addresses can ever hold the asset.\r\n\r\nValuation is stored in USD cents (`i128`). Amounts are integer token units in the\r\ntoken's own `decimals` base.\r\n\r\n**Testnet:** `CBMCWLSQSWUTLUJFCNBHNBSXMUM3XU7NAQ5TSNERW4HA4ZZBYHLG4ECZ`\r\n\r\n## Data types\r\n\r\n`AssetMetadata` is the single instance record describing the token:\r\n\r\n| Field | Type | Notes |\r\n|-------|------|-------|\r\n| `name` | `String` | Human-readable asset name |\r\n| `symbol` | `String` | Ticker, e.g. `MLOFT` |\r\n| `asset_type` | `String` | `real_estate` \\| `invoice` \\| `commodity` |\r\n| `total_supply` | `i128` | Base units |\r\n| `decimals` | `u32` | |\r\n| `admin` | `Address` | Issuer; the only account that can mint/pause |\r\n| `compliance_contract` | `Address` | The gate consulted on transfer/mint |\r\n| `asset_description` | `String` | |\r\n| `valuation` | `i128` | USD cents |\r\n| `paused` | `bool` | |\r\n\r\n## Function reference\r\n\r\n### `initialize`\r\n\r\n```rust\r\nfn initialize(\r\n    env: Env, admin: Address, name: String, symbol: String,\r\n    asset_type: String, total_supply: i128, decimals: u32,\r\n    compliance_contract: Address, asset_description: String, valuation: i128,\r\n)\r\n```\r\n\r\nInitializes the token and mints the full `total_supply` to `admin`. The admin\r\n**must already be compliance-approved** to hold the initial supply.\r\n\r\n- **Auth:** `admin`\r\n- **Errors:** `AlreadyInitialized (1)`, `InvalidAmount (5)` if `total_supply < 0` or `valuation < 0`, `RecipientNotCompliant (8)` if the admin is not approved.\r\n\r\n### `transfer`\r\n\r\n```rust\r\nfn transfer(env: Env, from: Address, to: Address, amount: i128)\r\n```\r\n\r\nMoves `amount` from `from` to `to`. Both parties must be compliance-approved and\r\nthe token must not be paused.\r\n\r\n- **Auth:** `from`\r\n- **Errors:** `InvalidAmount (5)` if `amount <= 0`, `Paused (6)`, `SenderNotCompliant (7)`, `RecipientNotCompliant (8)`, `InsufficientBalance (4)`.\r\n\r\n<CalloutBox variant=\"compliance\">\r\n`transfer` performs **two** cross-contract calls into the compliance contract —\r\n`is_allowed(from)` and `is_allowed(to)` — before touching balances. This is the\r\non-chain transfer gate. See [How transfer gating works](/docs/compliance-guide#how-transfer-gating-works).\r\n</CalloutBox>\r\n\r\n### `mint`\r\n\r\n```rust\r\nfn mint(env: Env, admin: Address, to: Address, amount: i128)\r\n```\r\n\r\nMints new tokens to a compliance-approved recipient, increasing `total_supply`.\r\n\r\n- **Auth:** `admin` (must equal the stored admin)\r\n- **Errors:** `Unauthorized (3)`, `InvalidAmount (5)`, `Paused (6)`, `RecipientNotCompliant (8)`, `Overflow (9)`.\r\n\r\n### `burn`\r\n\r\n```rust\r\nfn burn(env: Env, from: Address, amount: i128)\r\n```\r\n\r\nBurns `amount` of the caller's own tokens, decreasing `total_supply`.\r\n\r\n- **Auth:** `from`\r\n- **Errors:** `InvalidAmount (5)`, `InsufficientBalance (4)`.\r\n\r\n### `balance` / `total_supply`\r\n\r\n```rust\r\nfn balance(env: Env, id: Address) -> i128\r\nfn total_supply(env: Env) -> i128\r\n```\r\n\r\nView functions. `balance` returns `0` for an unknown address.\r\n\r\n### `pause` / `unpause`\r\n\r\n```rust\r\nfn pause(env: Env, admin: Address)\r\nfn unpause(env: Env, admin: Address)\r\n```\r\n\r\nHalts or resumes all transfers and mints. **Auth:** `admin`. Errors: `Unauthorized (3)`.\r\n\r\n### `update_valuation`\r\n\r\n```rust\r\nfn update_valuation(env: Env, admin: Address, new_valuation: i128)\r\n```\r\n\r\nUpdates the recorded USD-cents valuation. **Auth:** `admin`. Errors: `Unauthorized (3)`, `InvalidAmount (5)` if negative.\r\n\r\n### `set_compliance`\r\n\r\n```rust\r\nfn set_compliance(env: Env, admin: Address, compliance: Address)\r\n```\r\n\r\nPoints the token at a different compliance contract. Because the token knows only\r\nthe compliance *interface* (`is_allowed`), the concrete contract can be swapped.\r\n**Auth:** `admin`.\r\n\r\n### `get_metadata`\r\n\r\n```rust\r\nfn get_metadata(env: Env) -> AssetMetadata\r\n```\r\n\r\nReturns the full metadata record.\r\n\r\n## Code example\r\n\r\nTransfer with the JS SDK, going through the compliance gate:\r\n\r\n```ts\r\n\r\n\r\nconst token = new Contract(\"CBMCWLSQ…4ECZ\");\r\nconst op = token.call(\r\n  \"transfer\",\r\n  new Address(from).toScVal(),\r\n  new Address(to).toScVal(),\r\n  nativeToScVal(10000n, { type: \"i128\" }), // 100.00 @ 2 decimals\r\n);\r\n// build → simulate → sign with Freighter → submit\r\n```\r\n\r\n## Events\r\n\r\n| Topic | Data | Emitted by |\r\n|-------|------|------------|\r\n| `(\"mint\", admin)` | `total_supply` | `initialize` (initial mint) |\r\n| `(\"mint\", to)` | `amount` | `mint` |\r\n| `(\"transfer\", from, to)` | `amount` | `transfer` |\r\n| `(\"burn\", from)` | `amount` | `burn` |\r\n| `(\"pause\",)` / `(\"unpause\",)` | `admin` | `pause` / `unpause` |\r\n| `(\"valuation\",)` | `new_valuation` | `update_valuation` |\r\n| `(\"setcomp\",)` | `compliance` | `set_compliance` |\r\n\r\n## Error codes\r\n\r\n| Code | Error Name | Description |\r\n|------|------------|-------------|\r\n| 1 | `AlreadyInitialized` | Contract already initialized. Callable only once. |\r\n| 3 | `Unauthorized` | Caller is not the admin. |\r\n| 4 | `InsufficientBalance` | Caller does not hold enough balance to transfer or burn. |\r\n| 5 | `InvalidAmount` | Amount is invalid (≤ 0, exceeds bounds, or negative valuation). |\r\n| 6 | `Paused` | Token transfers and mints are paused. |\r\n| 7 | `SenderNotCompliant` | Sender failed the compliance gate. |\r\n| 8 | `RecipientNotCompliant` | Recipient failed the compliance gate. |\r\n| 9 | `Overflow` | Minting would exceed maximum supply. |\r\n\r\n## Security considerations\r\n\r\n- **Admin is fully trusted** for minting, pausing, valuation and swapping the\r\n  compliance contract. Use a secure key or multisig for the admin account.\r\n- **Gate coupling:** transfers depend on the compliance contract being live and\r\n  correct. `set_compliance` is powerful — pointing at a permissive contract would\r\n  disable the gate. Restrict admin access accordingly.\r\n- **Integer amounts:** all amounts are integers in the token's `decimals` base;\r\n  there is no floating point. Reject `amount <= 0` (the contract does).\r\n- **Pause is a kill-switch:** while paused, no value can move. This is intentional\r\n  for incident response and regulatory holds.\r\n"},{"id":10,"route":"/docs/contracts/compliance","title":"Compliance Contract","headers":"Compliance Contract Data types Function reference `initialize` `add_to_allowlist` `suspend` `remove` `is_allowed` `get_record` / `get_allowlist` `block_jurisdiction` / `unblock_jurisdiction` / `is_jurisdiction_blocked` Code example Events Error codes Security considerations","content":"\r\n\r\n# Compliance Contract\r\n\r\nMaintains the KYC allowlist and jurisdiction rules that gate who may hold or\r\ntransfer a tokenized asset. The asset-token contract calls\r\n[`is_allowed`](#is-allowed) on every transfer (for both sender and recipient) and\r\non every mint (for the recipient).\r\n\r\nTime is expressed in **ledger sequence numbers** (`u32`), not wall-clock dates. An\r\n`expires_at` of `0` means the approval never expires.\r\n\r\n**Testnet:** `CBUERYDM7DXTZLLKDBRJKUBPFJ7M4OSUN4T7XKUARU345RLXNAIQD2IU`\r\n\r\n## Data types\r\n\r\n`ComplianceStatus` — `Approved` | `Pending` | `Rejected` | `Suspended`.\r\n\r\n`KycRecord`:\r\n\r\n| Field | Type | Notes |\r\n|-------|------|-------|\r\n| `address` | `Address` | The KYC'd account |\r\n| `status` | `ComplianceStatus` | Only `Approved` passes the gate |\r\n| `jurisdiction` | `String` | ISO country code, e.g. `US`, `KE`, `DE` |\r\n| `verified_at` | `u32` | Ledger at verification |\r\n| `expires_at` | `u32` | Ledger of expiry; `0` = never |\r\n\r\n## Function reference\r\n\r\n### `initialize`\r\n\r\n```rust\r\nfn initialize(env: Env, admin: Address)\r\n```\r\n\r\nSets the admin. Callable once. **Auth:** `admin`. Errors: `AlreadyInitialized (1)`.\r\n\r\n### `add_to_allowlist`\r\n\r\n```rust\r\nfn add_to_allowlist(\r\n    env: Env, admin: Address, address: Address,\r\n    jurisdiction: String, expires_at: u32,\r\n)\r\n```\r\n\r\nAdds (or re-approves) an address, setting its status to `Approved`. If\r\n`expires_at` is non-zero it must be a future ledger.\r\n\r\n- **Auth:** `admin`\r\n- **Errors:** `Unauthorized (5)`, `InvalidExpiry (4)` if `expires_at != 0 && expires_at <= now`.\r\n\r\n### `suspend`\r\n\r\n```rust\r\nfn suspend(env: Env, admin: Address, address: Address)\r\n```\r\n\r\nSets an address's status to `Suspended`. The record is retained but `is_allowed`\r\nreturns `false` until it is re-approved. **Auth:** `admin`. Errors: `RecordNotFound (3)`.\r\n\r\n### `remove`\r\n\r\n```rust\r\nfn remove(env: Env, admin: Address, address: Address)\r\n```\r\n\r\nDeletes the record entirely and drops the address from the allowlist. **Auth:**\r\n`admin`. Errors: `RecordNotFound (3)`.\r\n\r\n### `is_allowed`\r\n\r\n```rust\r\nfn is_allowed(env: Env, address: Address) -> bool\r\n```\r\n\r\nThe core gate. Returns `true` **only if** the address is `Approved`, **and** not\r\nexpired (`expires_at == 0 || now < expires_at`), **and** its jurisdiction is not\r\nblocked. Returns `false` for unknown addresses.\r\n\r\n<CalloutBox variant=\"compliance\" title=\"This is the gate\">\r\nEvery check the RWA token performs funnels through `is_allowed`. Understanding its\r\nthree conditions — status, expiry, jurisdiction — is understanding the whole\r\ncompliance model. See the [Compliance Guide](/docs/compliance-guide).\r\n</CalloutBox>\r\n\r\n### `get_record` / `get_allowlist`\r\n\r\n```rust\r\nfn get_record(env: Env, address: Address) -> Option<KycRecord>\r\nfn get_allowlist(env: Env) -> Vec<Address>\r\n```\r\n\r\n`get_record` returns `None` for an unknown address. `get_allowlist` returns every\r\naddress currently on the allowlist (any status except removed).\r\n\r\n### `block_jurisdiction` / `unblock_jurisdiction` / `is_jurisdiction_blocked`\r\n\r\n```rust\r\nfn block_jurisdiction(env: Env, admin: Address, jurisdiction: String)\r\nfn unblock_jurisdiction(env: Env, admin: Address, jurisdiction: String)\r\nfn is_jurisdiction_blocked(env: Env, jurisdiction: String) -> bool\r\n```\r\n\r\nBlocking a jurisdiction makes **every** approved address in that jurisdiction fail\r\n`is_allowed`, without touching individual records. **Auth:** admin for the mutating\r\ncalls.\r\n\r\n## Code example\r\n\r\nApprove an investor in the US with a one-year expiry (~6.3M ledgers at 5s each):\r\n\r\n```ts\r\nconst compliance = new Contract(\"CBUERYDM…D2IU\");\r\nconst op = compliance.call(\r\n  \"add_to_allowlist\",\r\n  new Address(admin).toScVal(),\r\n  new Address(investor).toScVal(),\r\n  nativeToScVal(\"US\", { type: \"string\" }),\r\n  nativeToScVal(currentLedger + 6_300_000, { type: \"u32\" }),\r\n);\r\n```\r\n\r\n## Events\r\n\r\n| Topic | Data | Emitted by |\r\n|-------|------|------------|\r\n| `(\"approved\", address)` | `(jurisdiction, expires_at)` | `add_to_allowlist` |\r\n| `(\"suspend\", address)` | `()` | `suspend` |\r\n| `(\"removed\", address)` | `()` | `remove` |\r\n| `(\"blockjur\",)` | `jurisdiction` | `block_jurisdiction` |\r\n| `(\"unblkjur\",)` | `jurisdiction` | `unblock_jurisdiction` |\r\n\r\n## Error codes\r\n\r\n| Code | Error Name | Description |\r\n|------|------------|-------------|\r\n| 1 | `AlreadyInitialized` | Contract already initialized. Callable only once. |\r\n| 3 | `RecordNotFound` | Address record does not exist on the allowlist. |\r\n| 4 | `InvalidExpiry` | Expiry ledger is in the past or invalid. |\r\n| 5 | `Unauthorized` | Caller is not the admin. |\r\n\r\n## Security considerations\r\n\r\n- **Admin is the compliance authority.** Whoever holds the admin key decides who\r\n  can hold the asset. Protect it with multisig and clear operational controls.\r\n- **Expiry is enforced at read time** against the current ledger — an expired\r\n  approval fails the gate immediately, with no cron job required.\r\n- **Jurisdiction blocks are retroactive and immediate:** blocking `US` instantly\r\n  freezes every `US` holder's ability to transfer. Use deliberately.\r\n- **`remove` vs `suspend`:** `suspend` is reversible and keeps the audit record;\r\n  `remove` erases it. Prefer `suspend` for temporary actions.\r\n"},{"id":11,"route":"/docs/contracts/dividend","title":"Dividend Contract","headers":"Dividend Contract Data types Function reference `initialize` `create_distribution` `claimable` `claim` `get_distribution` / `get_distributions_for_asset` / `has_claimed` Code example Events Error codes Security considerations","content":"\r\n\r\n# Dividend Contract\r\n\r\nDistributes yield/dividends to asset-token holders in proportion to their\r\nholdings. An issuer funds a distribution with a payment token; each holder then\r\nclaims `total_amount * balance / total_supply`, paid from the escrow this contract\r\nholds. Each holder can claim a given distribution once.\r\n\r\nBalances are read **at claim time** from the asset token.\r\n\r\n**Testnet:** `CAR4XY3CEBQWFOL27JEWFW34KXSIZA7RFKDQMEIV7ZU723RWY37I2SYX`\r\n\r\n## Data types\r\n\r\n`Distribution`:\r\n\r\n| Field | Type | Notes |\r\n|-------|------|-------|\r\n| `id` | `u64` | Assigned sequentially |\r\n| `asset_token` | `Address` | The asset being paid out on |\r\n| `payment_token` | `Address` | The token used to pay (e.g. USDC) |\r\n| `total_amount` | `i128` | Pool size in payment-token base units |\r\n| `distributed` | `i128` | Claimed so far |\r\n| `created_at` | `u32` | Ledger at creation |\r\n| `completed` | `bool` | `true` once fully claimed |\r\n\r\n## Function reference\r\n\r\n### `initialize`\r\n\r\n```rust\r\nfn initialize(env: Env, admin: Address)\r\n```\r\n\r\nSets the admin. Callable once. **Auth:** `admin`. Errors: `AlreadyInitialized (1)`.\r\n\r\n### `create_distribution`\r\n\r\n```rust\r\nfn create_distribution(\r\n    env: Env, admin: Address, asset_token: Address,\r\n    payment_token: Address, total_amount: i128,\r\n) -> u64\r\n```\r\n\r\nCreates and funds a distribution, pulling `total_amount` of `payment_token` from\r\nthe admin into the contract's escrow. Returns the distribution id.\r\n\r\n- **Auth:** `admin` (must equal the stored admin; the transfer also requires the admin's authorization)\r\n- **Errors:** `Unauthorized (3)`, `InvalidAmount (5)` if `total_amount <= 0`.\r\n\r\n<CalloutBox variant=\"warning\">\r\nThe admin must hold at least `total_amount` of the payment token and the transfer\r\nmust succeed, or `create_distribution` reverts. Funds are escrowed in the dividend\r\ncontract until claimed.\r\n</CalloutBox>\r\n\r\n### `claimable`\r\n\r\n```rust\r\nfn claimable(env: Env, distribution_id: u64, holder: Address) -> i128\r\n```\r\n\r\nReturns the amount a holder can still claim: `0` if already claimed, if they hold\r\nnothing, or if the distribution is empty. Otherwise\r\n`total_amount * balance / total_supply` (floored by integer division).\r\n\r\n### `claim`\r\n\r\n```rust\r\nfn claim(env: Env, distribution_id: u64, holder: Address)\r\n```\r\n\r\nPays the holder's proportional share from escrow and marks it claimed.\r\n\r\n- **Auth:** `holder`\r\n- **Errors:** `DistributionNotFound (4)`, `AlreadyClaimed (7)`, `NothingToClaim (6)`.\r\n\r\n### `get_distribution` / `get_distributions_for_asset` / `has_claimed`\r\n\r\n```rust\r\nfn get_distribution(env: Env, distribution_id: u64) -> Distribution\r\nfn get_distributions_for_asset(env: Env, asset_token: Address) -> Vec<Distribution>\r\nfn has_claimed(env: Env, distribution_id: u64, holder: Address) -> bool\r\n```\r\n\r\nView helpers. `get_distributions_for_asset` backs the API's `/assets/:id/dividends`\r\nendpoint.\r\n\r\n## Code example\r\n\r\n```ts\r\nconst dividend = new Contract(\"CAR4XY3C…2SYX\");\r\n// issuer funds a 10,000-unit distribution in the payment token\r\nconst create = dividend.call(\r\n  \"create_distribution\",\r\n  new Address(admin).toScVal(),\r\n  new Address(assetToken).toScVal(),\r\n  new Address(paymentToken).toScVal(),\r\n  nativeToScVal(10_000_0000000n, { type: \"i128\" }), // 10,000 @ 7 decimals\r\n);\r\n\r\n// a holder later claims their share\r\nconst claim = dividend.call(\r\n  \"claim\",\r\n  nativeToScVal(1n, { type: \"u64\" }),\r\n  new Address(holder).toScVal(),\r\n);\r\n```\r\n\r\n## Events\r\n\r\n| Topic | Data | Emitted by |\r\n|-------|------|------------|\r\n| `(\"created\", admin)` | `(id, total_amount)` | `create_distribution` |\r\n| `(\"claim\", holder)` | `(distribution_id, amount)` | `claim` |\r\n\r\n## Error codes\r\n\r\n| Code | Error Name | Description |\r\n|------|------------|-------------|\r\n| 1 | `AlreadyInitialized` | Contract already initialized. Callable only once. |\r\n| 3 | `Unauthorized` | Caller is not the admin. |\r\n| 4 | `DistributionNotFound` | Distribution does not exist. |\r\n| 5 | `InvalidAmount` | Amount is invalid (≤ 0). |\r\n| 6 | `NothingToClaim` | Holder has no claimable amount in this distribution. |\r\n| 7 | `AlreadyClaimed` | Holder has already claimed from this distribution. |\r\n\r\n## Security considerations\r\n\r\n- **Balances are read at claim time**, not at snapshot. A holder who acquires more\r\n  tokens after a distribution is created — but before claiming — claims on the\r\n  larger balance. Issuers should be aware of this when timing distributions, or\r\n  pause the token around distribution windows.\r\n- **Integer division floors** each share, so a tiny residual may remain in escrow\r\n  after everyone claims; `completed` flips once `distributed >= total_amount`.\r\n- **Escrow custody:** funds live in the dividend contract between creation and\r\n  claim. The contract only pays out via `claim`; there is no admin withdrawal path.\r\n- **Payment-token decimals differ** from the asset token. Amounts here are in the\r\n  payment token's base units (7 for classic Stellar assets / SACs).\r\n"},{"id":12,"route":"/docs/contracts/registry","title":"Registry Contract","headers":"Registry Contract Data types Function reference `initialize` `register_asset` `get_asset` `get_assets_by_issuer` / `get_assets_by_type` / `get_all_assets` `deactivate_asset` `total_value_locked` / `asset_count` Code example Events Error codes Security considerations","content":"\r\n\r\n# Registry Contract\r\n\r\nA canonical, on-chain index of every tokenized asset on the platform. Each issuer\r\nregisters their asset-token contract here; the registry assigns a monotonically\r\nincreasing id and tracks issuer, type, valuation and active status. It also reports\r\ntotal value locked (TVL) across active assets.\r\n\r\n**Testnet:** `CBX5SMLTXX6JP4HA5GQIO2V6QM7WCUGL2GZ6D4U773HMRI6RXISKPUR3`\r\n\r\n## Data types\r\n\r\n`AssetEntry`:\r\n\r\n| Field | Type | Notes |\r\n|-------|------|-------|\r\n| `id` | `u64` | Assigned sequentially from 1 |\r\n| `token_contract` | `Address` | The asset-token contract |\r\n| `issuer` | `Address` | Who registered it |\r\n| `name` | `String` | |\r\n| `asset_type` | `String` | `real_estate` \\| `invoice` \\| `commodity` |\r\n| `valuation` | `i128` | USD cents |\r\n| `created_at` | `u32` | Ledger at registration |\r\n| `active` | `bool` | Excluded from TVL when `false` |\r\n\r\n## Function reference\r\n\r\n### `initialize`\r\n\r\n```rust\r\nfn initialize(env: Env, admin: Address)\r\n```\r\n\r\nSets the admin and resets the counter. Callable once. **Auth:** `admin`. Errors: `AlreadyInitialized (1)`.\r\n\r\n### `register_asset`\r\n\r\n```rust\r\nfn register_asset(\r\n    env: Env, issuer: Address, token_contract: Address,\r\n    name: String, asset_type: String, valuation: i128,\r\n) -> u64\r\n```\r\n\r\nRegisters a new asset and returns its assigned id.\r\n\r\n- **Auth:** `issuer`\r\n- **Errors:** `NotInitialized (2)`, `InvalidValuation (5)` if `valuation < 0`.\r\n\r\n### `get_asset`\r\n\r\n```rust\r\nfn get_asset(env: Env, asset_id: u64) -> AssetEntry\r\n```\r\n\r\nFetches a single asset. **Errors:** `AssetNotFound (4)`.\r\n\r\n### `get_assets_by_issuer` / `get_assets_by_type` / `get_all_assets`\r\n\r\n```rust\r\nfn get_assets_by_issuer(env: Env, issuer: Address) -> Vec<AssetEntry>\r\nfn get_assets_by_type(env: Env, asset_type: String) -> Vec<AssetEntry>\r\nfn get_all_assets(env: Env) -> Vec<AssetEntry>\r\n```\r\n\r\nQuery helpers returning filtered or complete lists. These back the API's\r\n`/assets` endpoint and the web app's explore page.\r\n\r\n### `deactivate_asset`\r\n\r\n```rust\r\nfn deactivate_asset(env: Env, admin: Address, asset_id: u64)\r\n```\r\n\r\nMarks an asset inactive; it is then excluded from TVL. **Auth:** `admin`. Errors:\r\n`Unauthorized (3)`, `AssetNotFound (4)`.\r\n\r\n### `total_value_locked` / `asset_count`\r\n\r\n```rust\r\nfn total_value_locked(env: Env) -> i128\r\nfn asset_count(env: Env) -> u64\r\n```\r\n\r\n`total_value_locked` sums `valuation` across **active** assets, in USD cents.\r\n`asset_count` returns the total registered (active or not).\r\n\r\n## Code example\r\n\r\n```ts\r\nconst registry = new Contract(\"CBX5SMLT…PUR3\");\r\n// register a freshly deployed asset token\r\nconst op = registry.call(\r\n  \"register_asset\",\r\n  new Address(issuer).toScVal(),\r\n  new Address(tokenContract).toScVal(),\r\n  nativeToScVal(\"Manhattan Loft\", { type: \"string\" }),\r\n  nativeToScVal(\"real_estate\", { type: \"string\" }),\r\n  nativeToScVal(500_000_000n, { type: \"i128\" }), // $5,000,000 in cents\r\n);\r\n```\r\n\r\n## Events\r\n\r\n| Topic | Data | Emitted by |\r\n|-------|------|------------|\r\n| `(\"register\", issuer)` | `id` | `register_asset` |\r\n| `(\"deactvate\",)` | `asset_id` | `deactivate_asset` |\r\n\r\n## Error codes\r\n\r\n| Code | Error Name | Description |\r\n|------|------------|-------------|\r\n| 1 | `AlreadyInitialized` | Contract already initialized. Callable only once. |\r\n| 2 | `NotInitialized` | Contract has not been initialized. |\r\n| 3 | `Unauthorized` | Caller is not the admin. |\r\n| 4 | `AssetNotFound` | Asset does not exist in the registry. |\r\n| 5 | `InvalidValuation` | Valuation is negative or invalid. |\r\n\r\n## Security considerations\r\n\r\n- **The registry is an index, not a source of truth for balances.** Supply and\r\n  balances live in the asset-token contract; the registry's `valuation` is the\r\n  issuer-declared figure at registration (updatable on the token via\r\n  `update_valuation`, not mirrored here automatically).\r\n- **Anyone can register** an asset (auth is the issuer, not the admin). Consumers\r\n  should treat listing in the registry as *self-attestation*, and rely on the\r\n  compliance contract for the actual holding restrictions.\r\n- **Deactivation is admin-only** and only affects TVL and listing, not the token.\r\n"},{"id":13,"route":"/docs/getting-started","title":"Getting Started","headers":"Getting Started What \"tokenizing a real-world asset\" means How the pieces fit together Prerequisites Quickstart 1. Clone and start the API → listening on 0.0.0.0:8080 2. Hit the live endpoints 3. Run the docs site Environment variables Running with Docker Network guide Deployed contract addresses (Testnet)","content":"\r\n\r\n# Getting Started\r\n\r\nThe **Stellar RWA Toolkit** is an end-to-end system for tokenizing real-world\r\nassets (RWAs) — real estate, invoices, commodities — on Stellar using Soroban\r\nsmart contracts, with **compliance enforced on-chain**.\r\n\r\nIt has three parts, each in its own repository:\r\n\r\n- **Contracts** — four Soroban contracts: `asset-token`, `compliance`, `registry`, `dividend`.\r\n- **API** — a Rust/Axum service that indexes all tokenized-asset activity and serves it over REST.\r\n- **Web app** — a Next.js front-end for issuers and investors.\r\n\r\n## What \"tokenizing a real-world asset\" means\r\n\r\nA real-world asset — say a $5,000,000 Manhattan loft — is represented on-chain as\r\na fungible token. Each token is a fractional claim on the underlying asset. Unlike\r\nan ordinary token, an RWA token is **compliance-gated**: only addresses that have\r\npassed KYC and sit on an allowlist may hold or receive it.\r\n\r\nThe gate is not a policy sitting in a database — it is enforced by the contract\r\nitself. On every transfer, the asset-token contract makes a cross-contract call\r\ninto the compliance contract to check both the sender and the recipient. If either\r\nfails, the transfer reverts.\r\n\r\n<CalloutBox variant=\"compliance\" title=\"Why this matters\">\r\nSecurities laws in most jurisdictions restrict who may hold a regulated asset.\r\nEnforcing that restriction **in the contract** — rather than in an app that can be\r\nbypassed — is what makes a token suitable to represent a regulated real-world\r\nasset. See the [Compliance Guide](/docs/compliance-guide).\r\n</CalloutBox>\r\n\r\n## How the pieces fit together\r\n\r\n```\r\n                         ┌───────────────┐\r\n   issuer / investor ──▶ │  Web app      │ ──▶ Freighter ──▶ Soroban contracts\r\n                         └───────────────┘                       │\r\n                                                                 │ events + state\r\n                         ┌───────────────┐   polls Soroban RPC   │\r\n   analysts / bots  ──▶  │  REST API     │ ◀─────────────────────┘\r\n                         └───────────────┘\r\n```\r\n\r\n- The **web app** signs and submits transactions through the user's wallet.\r\n- The **API** only reads: it indexes on-chain state every 10 seconds and never\r\n  holds a key or signs anything.\r\n\r\n## Prerequisites\r\n\r\n- [Freighter](https://freighter.app) browser extension (for the web app)\r\n- Node.js 18+ (web app and docs)\r\n- Rust 1.75+ (API and contracts)\r\n- The [Stellar CLI](https://developers.stellar.org/docs/tools/cli) (to deploy contracts)\r\n\r\n## Quickstart\r\n\r\nThe fastest way to see the API working is to run it locally against the deployed\r\nTestnet contracts — no wallet, no signing, no tokens required.\r\n\r\n### 1. Clone and start the API\r\n\r\n```bash\r\ngit clone https://github.com/A4-Stellar/Tessera\r\ncd Tessera/api\r\ncp .env.example .env      # Testnet contract IDs and RPC URL are pre-filled\r\ncargo run\r\n# → listening on 0.0.0.0:8080\r\n```\r\n\r\nThe API is ready when you see `listening on 0.0.0.0:8080` in the terminal.\r\nIt will start polling Soroban Testnet immediately; the first snapshot is\r\navailable within ~10 seconds.\r\n\r\n### 2. Hit the live endpoints\r\n\r\nOpen a second terminal and run these commands against `http://localhost:8080`:\r\n\r\n**Check the liveness probe:**\r\n\r\n```bash\r\ncurl http://localhost:8080/health\r\n```\r\n\r\n```json\r\n{\"status\":\"ok\"}\r\n```\r\n\r\n**Fetch platform-wide stats:**\r\n\r\n```bash\r\ncurl http://localhost:8080/stats\r\n```\r\n\r\n```json\r\n{\r\n  \"total_assets\": 1,\r\n  \"active_assets\": 1,\r\n  \"tvl_cents\": \"500000000\",\r\n  \"tvl_usd\": 5000000.0,\r\n  \"total_holders\": 1,\r\n  \"total_distributions\": 0,\r\n  \"last_indexed_ledger\": 3514152,\r\n  \"last_updated\": \"2026-07-09T08:43:12.101772791+00:00\"\r\n}\r\n```\r\n\r\n`tvl_cents` is a string to avoid JavaScript precision loss; divide by 100 for\r\ndollars. `last_updated` is the wall-clock time of the most recent successful\r\nSoroban RPC poll.\r\n\r\n**List all tokenized assets:**\r\n\r\n```bash\r\ncurl http://localhost:8080/assets\r\n```\r\n\r\n```json\r\n[\r\n  {\r\n    \"id\": 1,\r\n    \"contract_id\": \"CBMCWLSQSWUTLUJFCNBHNBSXMUM3XU7NAQ5TSNERW4HA4ZZBYHLG4ECZ\",\r\n    \"name\": \"Manhattan Loft\",\r\n    \"symbol\": \"MLOFT\",\r\n    \"asset_type\": \"real_estate\",\r\n    \"decimals\": 7,\r\n    \"total_supply\": \"50000000000000\",\r\n    \"valuation_cents\": \"500000000\",\r\n    \"valuation_usd\": 5000000.0,\r\n    \"active\": true\r\n  }\r\n]\r\n```\r\n\r\n**Fetch the sample asset (id `1`) directly:**\r\n\r\n```bash\r\ncurl http://localhost:8080/assets/1\r\n```\r\n\r\n```json\r\n{\r\n  \"id\": 1,\r\n  \"contract_id\": \"CBMCWLSQSWUTLUJFCNBHNBSXMUM3XU7NAQ5TSNERW4HA4ZZBYHLG4ECZ\",\r\n  \"name\": \"Manhattan Loft\",\r\n  \"symbol\": \"MLOFT\",\r\n  \"asset_type\": \"real_estate\",\r\n  \"decimals\": 7,\r\n  \"total_supply\": \"50000000000000\",\r\n  \"valuation_cents\": \"500000000\",\r\n  \"valuation_usd\": 5000000.0,\r\n  \"active\": true\r\n}\r\n```\r\n\r\n**Check token holders for asset `1`:**\r\n\r\n```bash\r\ncurl http://localhost:8080/assets/1/holders\r\n```\r\n\r\n```json\r\n[\r\n  {\r\n    \"address\": \"GAIQGTOBTTLLDJ4SWGGESM7UWJ2DI4K3ZNHUSHPDKJL2IE5FKY3BSRAA\",\r\n    \"balance\": \"50000000000000\",\r\n    \"share_percent\": 100.0\r\n  }\r\n]\r\n```\r\n\r\n**Check the compliance summary for asset `1`:**\r\n\r\n```bash\r\ncurl http://localhost:8080/assets/1/compliance\r\n```\r\n\r\n```json\r\n{\r\n  \"asset_id\": 1,\r\n  \"total_approved\": 1,\r\n  \"total_suspended\": 0,\r\n  \"total_expired\": 0\r\n}\r\n```\r\n\r\n<CalloutBox variant=\"note\">\r\nYour `last_indexed_ledger` and `last_updated` values will differ from the\r\nexamples above — they reflect the ledger height at the time your local indexer\r\nran. Everything else should match the sample Testnet deployment.\r\n</CalloutBox>\r\n\r\n### 3. Run the docs site\r\n\r\n```bash\r\ncd ../docs\r\nnpm install\r\nnpm run dev               # http://localhost:3000\r\n```\r\n\r\n### Environment variables\r\n\r\nThe API reads the following environment variables:\r\n\r\n| Variable | Default | Description |\r\n|----------|---------|-------------|\r\n| `RWA_RPC_URL` | `https://soroban-testnet.stellar.org` | Soroban RPC endpoint the indexer polls |\r\n| `RWA_CORS_ALLOWED_ORIGINS` | `http://localhost:3000` | Comma-separated browser origin allowlist |\r\n| `RWA_RATE_LIMIT_PER_SECOND` | `5` | Sustained requests allowed per client IP each second |\r\n| `RWA_RATE_LIMIT_BURST` | `20` | Maximum per-client request burst |\r\n| `RWA_REQUEST_TIMEOUT_SECS` | `30` | Maximum request duration in seconds |\r\n| `RWA_MAX_BODY_BYTES` | `1048576` | Maximum request body size in bytes |\r\n| `RWA_REGISTRY_ID` | `CBX5SMLTXX6JP4HA5GQIO2V6QM7WCUGL2GZ6D4U773HMRI6RXISKPUR3` | Deployed registry contract id (Testnet) |\r\n| `RWA_DIVIDEND_ID` | `CAR4XY3CEBQWFOL27JEWFW34KXSIZA7RFKDQMEIV7ZU723RWY37I2SYX` | Deployed dividend contract id (Testnet) |\r\n| `RWA_READ_SOURCE` | `GAIQGTOBTTLLDJ4SWGGESM7UWJ2DI4K3ZNHUSHPDKJL2IE5FKY3BSRAA` | Funded account used as source for read simulations (must exist on the RPC) |\r\n| `RWA_METRICS_TOKEN` | _(none)_ | Bearer token required by `/metrics`; requests are denied when unset |\r\n| `PORT` | `8080` | HTTP server port |\r\n| `RUST_LOG` | `tessera_api=info,tower_http=warn` | Log filter (tracing-subscriber format) |\r\n\r\nThe `.env.example` file includes Testnet defaults. Copy it and adjust as needed for your network.\r\n\r\n### Running with Docker\r\n\r\nBuild and run the API in a container:\r\n\r\n```bash\r\ncd Tessera/api\r\ndocker build -t tessera-api .\r\ndocker run -p 8080:8080 \\\r\n  -e RWA_RPC_URL=https://soroban-testnet.stellar.org \\\r\n  -e RWA_REGISTRY_ID=CBX5SMLTXX6JP4HA5GQIO2V6QM7WCUGL2GZ6D4U773HMRI6RXISKPUR3 \\\r\n  -e RWA_DIVIDEND_ID=CAR4XY3CEBQWFOL27JEWFW34KXSIZA7RFKDQMEIV7ZU723RWY37I2SYX \\\r\n  -e RWA_READ_SOURCE=GAIQGTOBTTLLDJ4SWGGESM7UWJ2DI4K3ZNHUSHPDKJL2IE5FKY3BSRAA \\\r\n  -e PORT=8080 \\\r\n  tessera-api\r\n```\r\n\r\nOr use an `.env` file:\r\n\r\n```bash\r\ndocker run -p 8080:8080 --env-file .env stellar-rwa-api\r\n```\r\n\r\nThen run the same curl commands from step 2 against `http://localhost:8080`.\r\n\r\n## Network guide\r\n\r\nEverything is deployed on **Testnet**. The network passphrase is:\r\n\r\n```\r\nTest SDF Network ; September 2015\r\n```\r\n\r\nPoint Freighter at Testnet and fund your account with the\r\n[friendbot](https://friendbot.stellar.org) before interacting.\r\n\r\n<CalloutBox variant=\"warning\">\r\nThese contracts are deployed on Testnet for demonstration. Do not use them to\r\ncustody real value. A mainnet deployment requires a full securities-compliance\r\nand legal review.\r\n</CalloutBox>\r\n\r\n## Deployed contract addresses (Testnet)\r\n\r\n| Contract    | Contract ID |\r\n|-------------|-------------|\r\n| compliance  | `CBUERYDM7DXTZLLKDBRJKUBPFJ7M4OSUN4T7XKUARU345RLXNAIQD2IU` |\r\n| asset-token | `CBMCWLSQSWUTLUJFCNBHNBSXMUM3XU7NAQ5TSNERW4HA4ZZBYHLG4ECZ` |\r\n| registry    | `CBX5SMLTXX6JP4HA5GQIO2V6QM7WCUGL2GZ6D4U773HMRI6RXISKPUR3` |\r\n| dividend    | `CAR4XY3CEBQWFOL27JEWFW34KXSIZA7RFKDQMEIV7ZU723RWY37I2SYX` |\r\n\r\nThe admin / issuer account for the sample deployment is\r\n`GAIQGTOBTTLLDJ4SWGGESM7UWJ2DI4K3ZNHUSHPDKJL2IE5FKY3BSRAA`, and the sample asset\r\nis **Manhattan Loft** (`MLOFT`), a `real_estate` asset valued at $5,000,000.\r\n\r\nContinue to the [Compliance Guide](/docs/compliance-guide) — the concept that\r\nmakes this an RWA platform rather than a plain token factory.\r\n"},{"id":14,"route":"/docs/integration","title":"Integration Guide","headers":"Integration Guide Querying the API platform stats all assets, then one asset's holders and dividends TypeScript client Reading contracts directly Submitting transactions Contract addresses","content":"\r\n\r\n# Integration Guide\r\n\r\nThree ways to integrate with the platform, from easiest to most direct:\r\n\r\n1. **Query the REST API** — indexed, JSON, no XDR.\r\n2. **Read contracts via the JS SDK** — live simulation of view calls.\r\n3. **Submit transactions** — sign with Freighter and invoke contract methods.\r\n\r\n## Querying the API\r\n\r\nThe [REST API](/docs/api/overview) is the simplest integration surface. It is\r\nread-only and returns plain JSON.\r\n\r\n<CalloutBox variant=\"note\">\r\nReplace <code>$API_BASE_URL</code> in the examples below with your configured base URL, or set the environment variable <code>NEXT_PUBLIC_API_BASE_URL</code>.\r\n</CalloutBox>\r\n\r\n```bash\r\n# platform stats\r\ncurl $API_BASE_URL/stats\r\n\r\n# all assets, then one asset's holders and dividends\r\ncurl $API_BASE_URL/assets\r\ncurl $API_BASE_URL/assets/1/holders\r\ncurl $API_BASE_URL/assets/1/dividends\r\n```\r\n\r\n### TypeScript client\r\n\r\n```ts\r\nconst API = process.env.NEXT_PUBLIC_API_BASE_URL ?? \"http://localhost:8080\";\r\n\r\nexport interface Asset {\r\n  id: number;\r\n  token_contract: string;\r\n  name: string;\r\n  symbol: string;\r\n  asset_type: string;\r\n  valuation_cents: string;\r\n  valuation_usd: number;\r\n  decimals: number;\r\n  total_supply: string;\r\n  holders: number;\r\n  active: boolean;\r\n  paused: boolean;\r\n  compliance_contract: string;\r\n}\r\n\r\nexport async function getAssets(): Promise<Asset[]> {\r\n  const res = await fetch(`${API}/assets`, { cache: \"no-store\" });\r\n  if (!res.ok) throw new Error(`API ${res.status}`);\r\n  return res.json();\r\n}\r\n\r\n// Amounts are strings (i128) — parse with BigInt and format by decimals.\r\nexport function displaySupply(a: Asset): string {\r\n  const raw = BigInt(a.total_supply);\r\n  const base = 10n ** BigInt(a.decimals);\r\n  return (Number(raw) / Number(base)).toLocaleString();\r\n}\r\n```\r\n\r\n<CalloutBox variant=\"tip\">\r\nAlways parse `valuation_cents`, `total_supply`, `balance`, and `total_amount` with\r\n<code>BigInt(...)</code> — they are decimal strings to preserve `i128` precision.\r\n</CalloutBox>\r\n\r\n## Reading contracts directly\r\n\r\nTo read live state without the API, simulate a view call with the Stellar JS SDK.\r\nNo signing is required for reads.\r\n\r\n```ts\r\n\r\n\r\nconst server = new rpc.Server(\"https://soroban-testnet.stellar.org\");\r\nconst REGISTRY = \"CBX5SMLTXX6JP4HA5GQIO2V6QM7WCUGL2GZ6D4U773HMRI6RXISKPUR3\";\r\n\r\nexport async function totalValueLocked(): Promise<bigint> {\r\n  const contract = new Contract(REGISTRY);\r\n  // Any valid account works as the simulation source.\r\n  const source = new Account(\r\n    \"GAIQGTOBTTLLDJ4SWGGESM7UWJ2DI4K3ZNHUSHPDKJL2IE5FKY3BSRAA\", \"0\",\r\n  );\r\n  const tx = new TransactionBuilder(source, {\r\n    fee: BASE_FEE, networkPassphrase: Networks.TESTNET,\r\n  })\r\n    .addOperation(contract.call(\"total_value_locked\"))\r\n    .setTimeout(30)\r\n    .build();\r\n\r\n  const sim = await server.simulateTransaction(tx);\r\n  if (rpc.Api.isSimulationError(sim)) throw new Error(sim.error);\r\n  return scValToNative(sim.result!.retval) as bigint; // USD cents\r\n}\r\n```\r\n\r\nCheck an address's compliance status the same way:\r\n\r\n```ts\r\n\r\n\r\nconst COMPLIANCE = \"CBUERYDM7DXTZLLKDBRJKUBPFJ7M4OSUN4T7XKUARU345RLXNAIQD2IU\";\r\n\r\nconst op = new Contract(COMPLIANCE).call(\r\n  \"is_allowed\",\r\n  new Address(investor).toScVal(),\r\n);\r\n// build → simulate → scValToNative(retval) === boolean\r\n```\r\n\r\n## Submitting transactions\r\n\r\nWrites must be signed by the account that authorizes them. In the browser, sign\r\nwith Freighter:\r\n\r\n```ts\r\n\r\n\r\n\r\nconst server = new rpc.Server(\"https://soroban-testnet.stellar.org\");\r\n\r\nexport async function transfer(\r\n  token: string, from: string, to: string, amount: bigint,\r\n): Promise<string> {\r\n  const account = await server.getAccount(from);\r\n  const op = new Contract(token).call(\r\n    \"transfer\",\r\n    new Address(from).toScVal(),\r\n    new Address(to).toScVal(),\r\n    nativeToScVal(amount, { type: \"i128\" }),\r\n  );\r\n  const built = new TransactionBuilder(account, {\r\n    fee: BASE_FEE, networkPassphrase: Networks.TESTNET,\r\n  }).addOperation(op).setTimeout(60).build();\r\n\r\n  // Simulate + assemble to attach the Soroban footprint and resource fees.\r\n  const sim = await server.simulateTransaction(built);\r\n  if (rpc.Api.isSimulationError(sim)) throw new Error(sim.error);\r\n  const prepared = rpc.assembleTransaction(built, sim).build();\r\n\r\n  const { signedTxXdr } = await signTransaction(prepared.toXDR(), {\r\n    networkPassphrase: Networks.TESTNET, address: from,\r\n  });\r\n  const signed = TransactionBuilder.fromXDR(signedTxXdr, Networks.TESTNET);\r\n  const sent = await server.sendTransaction(signed);\r\n  return sent.hash; // then poll server.getTransaction(hash)\r\n}\r\n```\r\n\r\n<CalloutBox variant=\"compliance\">\r\nA `transfer` will revert with <code>SenderNotCompliant (7)</code> or\r\n<code>RecipientNotCompliant (8)</code> if either party is not KYC-approved. Check\r\n<code>is_allowed</code> for both addresses before submitting to give users a clear\r\nerror up front.\r\n</CalloutBox>\r\n\r\n## Contract addresses\r\n\r\nAll Testnet contract ids are listed in [Getting Started](/docs/getting-started#deployed-contract-addresses-testnet).\r\nAsset-token contract ids are discovered from the registry's `get_all_assets` (or\r\nthe API's `/assets`).\r\n"},{"id":15,"route":"/docs/time-and-ledgers","title":"Time & Ledgers","headers":"Time & Ledgers Why ledger sequences? Ledger sequences in contracts The special case: `expires_at = 0` means \"never\" Checking expiry at runtime Ledger sequences in the API Assets endpoint Compliance endpoint Dividends endpoint Converting ledger numbers to wall-clock time Handling expires_at in your integration Common mistakes","content":"\r\n\r\n# Time & Ledgers\r\n\r\nThroughout the Stellar RWA contracts and API, time is never expressed as a wall-clock\r\ntimestamp. Instead, all time values use **Stellar ledger sequence numbers** — a monotonically\r\nincreasing counter that increments roughly every 5 seconds on the Stellar network.\r\n\r\nThis design eliminates time-sync issues and makes on-chain logic deterministic.\r\n\r\n## Why ledger sequences?\r\n\r\n- **No clock skew.** A ledger number is determined by network consensus, not server clocks.\r\n- **Deterministic ordering.** You can always compare ledger numbers to know which event happened\r\n  first, without worrying about timestamp precision.\r\n- **Simpler contracts.** No need to worry about wall-clock adjustments or leap seconds.\r\n\r\n## Ledger sequences in contracts\r\n\r\nAll on-chain time fields are `u32` ledger sequence numbers:\r\n\r\n- **Compliance contract:** `verified_at`, `expires_at` (approval expiry)\r\n- **Dividend contract:** `created_at` (when a distribution was announced)\r\n- **Registry contract:** `created_at` (when an asset was registered)\r\n- **Asset-token contract:** `clawback_at` (when a clawback was performed)\r\n\r\n### The special case: `expires_at = 0` means \"never\"\r\n\r\nWhen an expiry field (like `expires_at` in the compliance allowlist) is set to `0`, it means\r\nthe permission **never expires**. This is a universal convention across all contracts:\r\n\r\n```ts\r\n// Add a user to the allowlist with no expiry\r\nconst op = compliance.call(\r\n  \"add_to_allowlist\",\r\n  admin,\r\n  userAddress,\r\n  \"US\",\r\n  0  // expires_at = 0 means never expires\r\n);\r\n```\r\n\r\nTo set an expiry, provide a **future** ledger number:\r\n\r\n```ts\r\n\r\n\r\nconst server = new Horizon.Server(\"https://horizon.stellar.org\");\r\nconst ledgers = await server.ledgers().limit(1).order(\"desc\").call();\r\nconst now = ledgers.records[0].sequence;\r\n\r\n// Approve for 1 year (~6.3 million ledgers at ~5s per ledger)\r\nconst expiresAt = now + 6_300_000;\r\n\r\nconst op = compliance.call(\r\n  \"add_to_allowlist\",\r\n  admin,\r\n  userAddress,\r\n  \"US\",\r\n  expiresAt\r\n);\r\n```\r\n\r\n### Checking expiry at runtime\r\n\r\nThe compliance contract's `is_allowed` gate checks expiry like this:\r\n\r\n```rust\r\n// User is approved if:\r\n// (expires_at == 0)     -- never expires, OR\r\n// (now < expires_at)    -- current ledger is before expiry ledger\r\n```\r\n\r\nOnce `now >= expires_at`, the approval is considered expired and `is_allowed` returns `false`,\r\nblocking all transfers. No on-chain cron job is needed — expiry is enforced at read time.\r\n\r\n## Ledger sequences in the API\r\n\r\nThe REST API mirrors this design. All time fields in responses are **ledger sequence numbers**,\r\nnever wall-clock timestamps:\r\n\r\n### Assets endpoint\r\n\r\n```json\r\n{\r\n  \"id\": 1,\r\n  \"name\": \"Manhattan Loft\",\r\n  \"created_at_ledger\": 3502885,\r\n  \"total_supply\": \"1000000\"\r\n}\r\n```\r\n\r\n### Compliance endpoint\r\n\r\n```json\r\n{\r\n  \"address\": \"GBUQWP...\",\r\n  \"status\": \"Approved\",\r\n  \"jurisdiction\": \"US\",\r\n  \"verified_at\": 3500000,\r\n  \"expires_at\": 9800000,\r\n  \"expired\": false\r\n}\r\n```\r\n\r\nThe `expired` field is a convenience boolean: `true` if `expires_at != 0 && expires_at <= last_indexed_ledger`.\r\n\r\n### Dividends endpoint\r\n\r\n```json\r\n{\r\n  \"id\": 1,\r\n  \"asset_id\": 1,\r\n  \"per_share_cents\": \"100\",\r\n  \"created_at_ledger\": 3510000,\r\n  \"payable_at_ledger\": 3520000,\r\n  \"claimed\": 500\r\n}\r\n```\r\n\r\n## Converting ledger numbers to wall-clock time\r\n\r\nTo convert a ledger number to an approximate wall-clock time, use the Horizon API:\r\n\r\n```ts\r\nconst server = new Horizon.Server(\"https://horizon.stellar.org\");\r\nconst ledger = await server.ledgers().ledger(3502885).call();\r\n\r\nconsole.log(ledger.closed_at);  // \"2026-05-15T10:30:00Z\"\r\n```\r\n\r\nEach ledger also records the wall-clock time it was closed, which you can use to estimate\r\nwhen a given ledger sequence number occurred.\r\n\r\n**Rule of thumb:** Stellar produces ~1 ledger per 5 seconds, so a ledger 1 million in the\r\nfuture is roughly 58 days away.\r\n\r\n## Handling expires_at in your integration\r\n\r\nWhen building an integration against the API:\r\n\r\n1. **Display the ledger number and the `expired` boolean.** Don't try to calculate expiry\r\n   yourself; trust the `expired` field from the API.\r\n2. **Refresh approval before expiry.** If you're tracking approval expiry, call\r\n   `add_to_allowlist` again before the `expires_at` ledger is reached.\r\n3. **Remember `expires_at = 0` means never.** Don't treat `0` as an epoch or default timestamp.\r\n4. **Use the API's `expired` field.** The API already handles the comparison against the\r\n   current indexed ledger for you.\r\n\r\n## Common mistakes\r\n\r\n- **Treating ledger numbers as timestamps:** They are not microseconds, milliseconds, or any\r\n  other time unit. They are ledger sequence numbers only.\r\n- **Forgetting that `0` means \"never\":** Setting `expires_at: 0` will create an approval that\r\n  never expires, which may not be what you intended.\r\n- **Assuming expiry is checked by a cron job:** Expiry is enforced at read time (`is_allowed`).\r\n  Once the current ledger passes `expires_at`, the approval is immediately and deterministically\r\n  considered expired.\r\n- **Comparing ledger numbers across different chains:** Each Stellar network (testnet, public,\r\n  futurenet) has its own ledger sequence counter. Do not assume a ledger sequence is portable\r\n  between networks.\r\n"},{"id":16,"route":"/docs/web-app","title":"Web App Guide","headers":"Web App Guide Connecting Freighter Managing the compliance allowlist (issuer) Creating dividend distributions (issuer) Investor: compliance status and holdings Transaction feedback","content":"\r\n\r\n# Web App Guide\r\n\r\nThe web app is the front-end for issuers and investors. It signs and submits every\r\ntransaction through the user's **Freighter** wallet — it never holds keys. This\r\nguide walks through the main flows.\r\n\r\n## Connecting Freighter\r\n\r\n1. Install the [Freighter](https://freighter.app) browser extension and create or\r\n    approve the issuer address in the\r\ncompliance step first.\r\n</CalloutBox>\r\n\r\n## Managing the compliance allowlist (issuer)\r\n\r\nFrom the **Issuer → Compliance** page, select one of your assets:\r\n\r\n- **Add an address** — enter the account, choose a jurisdiction, and set an\r\n  optional expiry. This calls `add_to_allowlist` and sets the status to `Approved`.\r\n- **Suspend / remove** — temporarily hold or permanently delete an approval.\r\n- **Block a jurisdiction** — freeze every holder in a country at once.\r\n\r\nThe allowlist table shows each record's status, jurisdiction, and expiry. See the\r\n[Compliance Guide](/docs/compliance-guide) for the model behind these actions.\r\n\r\n## Creating dividend distributions (issuer)\r\n\r\nFrom **Issuer → Dividends**, select an asset and open **Create Distribution**:\r\n\r\n1. Choose the **payment token** (e.g. USDC on Stellar).\r\n2. Enter the **total amount** to distribute.\r\n3. Submit — the app calls `create_distribution`, which escrows the funds in the\r\n   dividend contract.\r\n\r\nHolders then claim their proportional share. Each distribution shows a progress bar\r\nof claimed vs. total.\r\n\r\n## Investor: compliance status and holdings\r\n\r\nAs an investor you can:\r\n\r\n- **Check your compliance status** on any asset's detail page and in your\r\n  **Portfolio**. The app reads `is_allowed` and your `KycRecord`, and shows\r\n  `Approved` / `Pending` / `Suspended` / `Rejected` / `Not Registered`.\r\n- **Hold and transfer** an asset — but only if you are approved. The transfer form\r\n  is **disabled** with an explanatory message whenever you are not KYC-approved, the\r\n  asset is paused, or your balance is zero.\r\n- **Claim dividends** — the app shows your claimable amount per distribution and a\r\n  one-click **Claim** button that calls `claim`.\r\n\r\n<CalloutBox variant=\"compliance\">\r\nIf your transfer is blocked, the app tells you why — pending approval, suspension,\r\na blocked jurisdiction, an expired approval, or simply not being on the allowlist.\r\nAsk the asset's issuer to approve your address. This is the compliance gate, not a\r\nbug.\r\n</CalloutBox>\r\n\r\n## Transaction feedback\r\n\r\nEvery on-chain action shows live progress — *preparing → awaiting signature →\r\nsubmitting → confirming* — and, on success, a link to the transaction on Stellar\r\nExpert. Contract errors are translated into plain-language messages.\r\n"}]